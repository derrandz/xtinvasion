package tests

// func TestFindAlienIndex(t *testing.T) {
// 	app := NewApp(3, 3)
// 	controller := NewController(app)

// 	// Find an alien that doesn't exist.
// 	if findAlienIndex(app.AlienLocations[app.WorldMap.Cities["Bar"]], 0) != -1 {
// 		t.Errorf("Expected alien index to be -1, got %d", findAlienIndex(app.AlienLocations[app.WorldMap.Cities["Bar"]], 0))
// 	}

// 	// Find an alien that exists.
// 	controller.MoveAlienToCity(0, "Bar")
// 	if findAlienIndex(app.AlienLocations[app.WorldMap.Cities["Bar"]], 0) == -1 {
// 		t.Errorf("Expected alien index to be 0, got %d", findAlienIndex(app.AlienLocations[app.WorldMap.Cities["Bar"]], 0))
// 	}
// }
